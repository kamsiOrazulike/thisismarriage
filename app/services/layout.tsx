export const metadata = {
  title: "Services - This is Marriage",
  description:
    "Learn about the services we offer to help couples thrive in their relationships.",
};

export default function ServicesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

export const metadata = {
  title: "Courses - This is Marriage",
  description:
    "Explore our courses designed to help couples build stronger relationships.",
};

export default function CoursesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
